import React from "react";
import ReactDOM from "react-dom/client";
import HealthRPG from "./HealthRPG.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HealthRPG />
  </React.StrictMode>
);
